<style>
  /* Animaciones del hero */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes slowZoom {
    0% {
      transform: scale(1.5);
    }
    100% {
      transform: scale(1.08);
    }
  }

  @keyframes shimmer {
    0% {
      background-position: -200% center;
    }
    100% {
      background-position: 200% center;
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  .hero-bg {
    animation: slowZoom 20s ease-out forwards;
  }

  .hero-title {
    opacity: 0;
    animation: fadeInUp 0.8s ease-out 0.2s forwards;
  }

  .hero-description {
    opacity: 0;
    animation: fadeInUp 0.8s ease-out 0.4s forwards;
  }

  .hero-cta {
    opacity: 0;
    animation: fadeInUp 0.8s ease-out 0.6s forwards;
  }

  .hero-badge {
    opacity: 0;
    animation: fadeInUp 0.8s ease-out 0s forwards;
  }

  .hero-scroll {
    opacity: 0;
    animation:
      fadeIn 1s ease-out 1s forwards,
      float 3s ease-in-out 2s infinite;
  }

  .text-shimmer {
    background: linear-gradient(
      90deg,
      #ffffff 0%,
      #ffffff 40%,
      #a8d5a2 50%,
      #ffffff 60%,
      #ffffff 100%
    );
    background-size: 200% auto;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: shimmer 4s linear infinite;
  }
</style>

<section
  id="inicio"
  class="relative min-h-[50vh] sm:min-h-screen overflow-hidden flex items-center"
>
  <!-- Imagen de fondo con efecto zoom -->
  <div
    class="hero-bg absolute inset-0 bg-cover bg-no-repeat"
    style="background-image: url('/lacelina2r.JPG'); background-position: 80% center;"
  >
  </div>

  <!-- Overlay verde sutil -->
  <div class="absolute inset-0 bg-[#234332]/10"></div>

  <!-- Gradiente radial para enfoque -->
  <div
    class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_rgba(35,67,50,0.05)_0%,_rgba(35,67,50,0.6)_80%,_rgba(35,67,50,0.9)_100%)]"
  >
  </div>

  <!-- Gradiente inferior para transición -->
  <div
    class="absolute bottom-0 left-0 w-full h-40 bg-gradient-to-t from-[#234332] to-transparent pointer-events-none"
  >
  </div>

  <!-- Contenido principal -->
  <div class="relative mx-auto max-w-7xl px-1 sm:px-8 py-16 w-full">
    <div class="w-full sm:w-1/2 text-white px-5">
      <div class="max-w-max">
        <!-- Badge animado -->

        <!-- Titulo con shimmer -->
        <h1
          class="hero-title text-4xl sm:text-5xl lg:text-6xl font-extrabold mb-6 leading-tight tracking-tight sm: py-8"
        >
          <span class="text-shimmer">Producción y comercialización</span>
          <br />
          <span class="text-white">agropecuaria</span>
        </h1>

        <!-- Descripción -->
        <p
          class="hero-description text-base sm:text-lg text-slate-200/90 leading-relaxed max-w-prose mb-8"
        >
          En Agrícola Ganadera Celina SA trabajamos con compromiso,
          transparencia y eficiencia para brindar soluciones confiables al
          sector agroindustrial.
        </p>

        <!-- CTA con mejoras -->
        <div class="hero-cta flex flex-wrap items-center gap-4">
          <a
            href="#contacto"
            class="group inline-flex items-center justify-center gap-2
             bg-gradient-to-r from-[#3E7C4C] to-[#5DA46A]
             text-white font-semibold rounded-xl
             px-7 py-3.5 shadow-lg shadow-[#3E7C4C]/30
             hover:from-[#5DA46A] hover:to-[#7FBA81]
             hover:scale-105 hover:shadow-xl hover:shadow-[#3E7C4C]/40
             active:scale-95
             transition-all duration-300"
          >
            Contactanos
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 transition-transform duration-300 group-hover:translate-x-1"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M5 12h14"></path>
              <path d="m12 5 7 7-7 7"></path>
            </svg>
          </a>

          <!-- Link secundario opcional -->
          <a
            href="#contacto"
            class="group inline-flex items-center gap-2 text-white/80 hover:text-white font-medium transition-colors duration-300"
          >
            <span
              class="w-10 h-10 rounded-full flex items-center justify-center transition-all duration-300 group-hover:scale-110"
              style="background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2);"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="w-4 h-4"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path
                  d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"
                ></path>
              </svg>
            </span>
            Llamanos
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Indicador de scroll -->
</section>
